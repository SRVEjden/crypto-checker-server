# Используем официальный образ Node.js на основе Alpine (легковесный)
FROM node:18-alpine

# Создаем рабочую директорию приложения
WORKDIR /usr/src/app

# Копируем ВСЕ файлы из текущей директории (где находится Dockerfile) в контейнер
COPY . .

# Устанавливаем зависимости
RUN npm install

# Собираем TypeScript проект (если нужно)
RUN npm run build

# Открываем порт, который использует приложение
EXPOSE 3001

# Команда для запуска приложения
CMD ["npm", "start"]